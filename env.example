# Gateway env example
# - docker-compose 사용 시: 이 파일을 참고해서 `.env.production`을 만들어 주세요.
# - `.env` 포맷 파서에 따라 따옴표 처리 방식이 다를 수 있으니,
#   가능하면 불필요한 바깥 따옴표는 피하고(특히 JSON), 값에 공백이 있을 때만 따옴표를 사용하세요.

## Runtime
NODE_ENV=production

## Database (필수)
# - docker-compose 기준: db 컨테이너를 사용하므로 host는 `db`
DATABASE_URL=postgresql://postgres:password@db:5432/gateway?schema=public

## Gateway 서비스 매핑 (필수, JSON)
# - host 헤더(도메인) 기준으로 업스트림을 찾습니다.
# - 예) `svc1.local`로 들어온 요청은 `http://nginx1:80`로 프록시
# - 주의: JSON 전체를 다시 따옴표로 감싸지 않는 것을 권장합니다.
GATEWAY_SERVICES={"svc1.local":{"upstream":"http://nginx1:80"},"svc2.local":{"upstream":"http://nginx2:80"}}

## Better Auth (필수)
# - 게이트웨이의 canonical origin (CORS origin, OAuth callback/redirect 구성에 사용)
# - 로컬 개발 예: http://localhost
BETTER_AUTH_URL=http://localhost
# - 세션/토큰 서명에 사용하는 시크릿 (반드시 강한 랜덤 값으로 교체)
BETTER_AUTH_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

## Email OTP (Resend) - 현재 서버 설정상 필요
# - 이메일 OTP(sign-in) 발송에 사용됩니다.
# - Resend 대시보드에서 발급 받은 API Key
RESEND_API_KEY=REPLACE_WITH_RESEND_API_KEY
# - 발신자 이메일(도메인 인증 필요할 수 있음). 공백/표시명 필요 시 따옴표 사용 가능
RESEND_FROM_EMAIL=no-reply@example.com

## Social Login (선택)
# - 실제로 GitHub/Google 로그인을 사용할 때만 채우세요.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=


